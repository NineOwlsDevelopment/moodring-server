# Environment Variables Example
#
# Vite automatically loads environment files based on the mode:
# - .env.development  → loaded when running `npm run dev` or `npm run build-dev`
# - .env.production   → loaded when running `npm run build`
# - .env.local        → loaded in all modes (highest priority, git-ignored)
#
# Copy this file to create your environment files:
#   cp example.env .env.development
#   cp example.env .env.production
#
# Then update the values for each environment.

VITE_API_URL=
VITE_WSS_URL=
